{
    "name": "Use VS Online to code in InterSystems ObjectScript in an IRIS container",
    "dockerFile": "Dockerfile",
    "appPort": [
        51773,
        52773
    ],
    "extensions": [
        "daimor.vscode-objectscript"
    ],

    // Suppress default 'oryx build'
    "postCreateCommand": ""
}